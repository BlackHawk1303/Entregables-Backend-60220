<div class="body">
    <div class="content">
        <div class="header">
            <a href="/products">Productos</a>
            <a id="enlace" href="#">Carrito</a>
            <a href="/chat">Chat</a>
            <button id="shutdownSession">Cerrar Sesión</button>
        </div>
        <div id="midiv"></div>
    </div>
</div>

<script src="/js/users.js"></script>

<style>
    /* Estilos Globales */
    body {
        font-family: 'Arial', sans-serif;
        margin: 0;
        padding: 0;
        background: #f4f4f4;
        color: #333;
    }

    /* Estilos ara el Contenedor Principal */
    .body {
        max-width: 800px;
        margin: 20px auto;
        padding: 20px;
        background: white;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

    .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-bottom: 10px;
        border-bottom: 1px solid #ccc;
        margin-bottom: 20px;
    }

    .header a {
        text-decoration: none;
        color: #007bff;
        font-weight: bold;
        margin-right: 20px;
    }

    .header a:last-child{
        margin-right: 0;
    }

    .header button {
        padding: 8px 16px;
        background-color: #dc3545;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        outline: none;
        margin-left: 20px;
    }

    #midiv {
        margin-top: 20px;
    }